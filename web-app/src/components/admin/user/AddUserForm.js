// /src/components/admin/users/AddUserForm.js
import React from 'react';
import UserForm from './UserForm';

const AddUserForm = ({ onSubmit }) => (
  <UserForm onSubmit={onSubmit} />
);

export default AddUserForm;
